description: VTI interface configuration.
type: object
properties:
  addressing:
    description: Address assignment method.
    type: string
    enum:
      - static
      - none
  address:
    description: IPv4 addresses in CIDR notation.
    type: array
    items:
      type: string
      format: uc-cidr4
